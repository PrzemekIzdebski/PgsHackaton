@model System.Collections.Generic.IEnumerable<PgsTwitter.Models.Tag.TagModel>

@{
    ViewBag.Title = "Index";
}

<h2>Tags</h2>

    <ul>
        @{
            foreach (var tag in Model)
            {
                <li>
                    @tag.Name : @tag.Count
                </li>
            }
        }

    </ul>